2026-03-30: 已停止 launch_abcd_5seeds（进程组 SIGTERM/KILL），后台重启：PID 3850893，日志 scripts/outputs/launch_abcd_5seeds_bg_20260330_174651.log
2026-03-30: 用户自取后台+日志命令：nohup python3 /root/25S151115/project3/scripts/launch_abcd_5seeds.py >> /root/25S151115/project3/scripts/outputs/launch_abcd_5seeds_$(date +%Y%m%d_%H%M%S).log 2>&1 & echo $!
2026-03-31: Conformer.py —— 「侧边」为 use_adapter→LocalMultiheadAttention 内 q/k/v/out 的 AdaptFFN；CA 为 CABlock，接在 Conv 后、FFN2 前。配置经 get_model.py 的 use_adapter / use_ca_block。
2026-03-31: 用户问 sider/CA：README/MODULE_FRAMEWORK 无此内容；实现见 scripts/src/model_a/models/Conformer.py + get_model.py；CLI main_pipeline 有 --use_ca_block。
2026-03-31: launch_abcd_5seeds_20260330_175435.log：255 任务 ok=252 failed=3（7-10 random seed3、7-9 random seed4、5-7 real_slpe seed4）。disk 上 random100/full_data 共目录 random/（finetune_module 未把 output_tag 传入 get_finetune_config）；results 见 outputs/model_test/{pre}-{day}/random|real_slpe/seed0-4。
2026-03-31: 按 model_test 各 N-M 目录：random100 用 experiment_info Num Samples==100，full 用 -1，real_slpe 独立目录；全局均值 random100≈0.368(n=30)、full≈0.342(n=56)、real_slpe≈0.365(n=84)；逐对数值见用户对话表。
2026-03-31: scripts/outputs/results.md — A/B/D real_slpe 表改为 seed0–4 Mean±Std，与 launch_abcd_5seeds 落盘一致；B day7 n=4。
2026-03-31: scripts/summarize_abcd_5seeds_logs.py + results.md 一节：从 logs_abcd_5seeds 汇总 random100/full（避免 random 目录覆盖）；缺 random100 C seed3 day10、seed4 day9。
2026-03-31: C组 c_suite_20260331（5seed×day8-12×5任务=125）全部完成：random100/random100_l2sp/full_data/real_slpe100/real_slpe100_l2sp 各25成功0失败；日志分目录写入 logs_abcd_5seeds/c_suite_20260331/{variant}/。full_data 训练句数为各day真实全量（280/320/360/480），非固定500。另修复 finetune_module：此前未透传 l2sp_lambda 等 finetune kwargs，导致“l2sp实验”实际未生效；现已透传。
2026-04-01: 已按天汇总 c_suite_20260331（day8-12）并展示；按修复后配置发起重跑 c_suite_rerun_fixl2sp_20260331（C组, 5seed, 5任务, GPU 0/1/2/3/4, 每卡并行4），主进程PID 1930605，日志根目录 scripts/outputs/automated_experiments/logs_abcd_5seeds/c_suite_rerun_fixl2sp_20260331。
2026-04-01: c_suite_rerun_fixl2sp_20260331 结果 ok=0/125（全部失败）。原因定位：finetune_module 透传 kwargs 时把 lrStart/lrEnd 的 None 覆盖默认值，导致训练失败。已修复为仅透传非 None 参数（extra_cfg 过滤 None），需重新发起新 tag 的重跑。
2026-04-01: 修复 real_slpe/random/full 的 Model A 一致性：finetune_module 在 get_finetune_config 后强制 config["pretrainedModelPath"]=model_a_path，确保“选句模型”和“微调初始化模型”完全一致，不再回落到 seed0。
2026-04-01: 已按用户要求“全部停止后重跑子集”：先停掉 launch_abcd_5seeds/main_pipeline finetune 相关进程；launcher 新增 --variants 以按任务类型筛选；现已启动 C组子集重跑（random100_l2sp + real_slpe100 + real_slpe100_l2sp，5seed，GPU 0/1/2/3/4，每卡并行4，tag=c_subset_fix3_20260401），主进程PID 2215622。
2026-04-01: 用户校正 day8–12 表：random100_l2sp / real_slpe100 / real_slpe100_l2sp 三列已按 C 组子集 PER DAY MEAN 汇总；± 由 min–max 极差经 d₂ 估 σ 再除 √n（n=4/5）；random100、full_data 沿用旧表未改。
2026-04-01: 排查 real_slpe 退化原因：SLPE 缓存由 conformer-7days（无 seed 后缀）计算，所有 seed 共用同一批样本。两处修复：①finetune_module.py 加 use_slpe_cache 参数（默认 False）；②sample_selection.py compute_real_slpe_scores 中缓存读取条件改为 slpe_cache_dir is not None（原来是 or 回退默认目录）。main_pipeline.py / launcher 同步加 --use_slpe_cache 参数。
2026-04-01: c_slpe_fix2_20260401 重跑完成（50/50 ok），real_slpe 确认按各 seed 的 model_a 重算（日志中无 Loading SLPE from cache）。新结果：day9-12 real_slpe100 明显优于 random100（最大 Δ=-0.024 on day11），day8 略差于 random（+0.013）。results.md 组C已更新。
2026-04-06: 对比上学期/本学期 real_slpe：day_8_slpe_scores.pkl（2026-01-19）与 day_8_train_slpe_scores.pkl（2026-01-20）为两套分数体系；real_slpe 读取的是无 train 文件。旧实验 7-8/real_slpe/seed0 的 selected_indices 与 day_8_slpe_scores 完全一致（Top100），而本学期重跑（const/linear）selected_indices 另一套，day8 seed0 CER 从 0.342246 变到约 0.356；旧/新 Top100 在 day8-12 的重合仅约 30%-47%。
2026-04-06: 多 seed 场景下，real_slpe 可按 seed 精确打分与选样，前提是 use_slpe_cache=False（默认）且 model_a_path 指向对应 seed 目录；若开启 use_slpe_cache=True，会读取共享 day_x_slpe_scores.pkl（不带 seed），不同 seed 可能共用同一批样本。
2026-04-06: 已梳理当前 SLPE 工作流：real_slpe 选样链路使用 sample_selection.compute_real_slpe_scores -> slpe.compute_slpe_batch，默认 use_slpe_cache=False 按 model_a_path 重算；Model B 标签链路使用 model_b_data_module.compute_scores_for_model_b，缓存文件命名为 day_X_train_slpe_scores.pkl / day_X_test_slpe_scores.pkl（或 days_6_7_*），与 real_slpe 的 day_X_slpe_scores.pkl 为不同体系。
2026-04-06: day8 实测重合率（以上学期文件 vs 当前重算 seed0）：old day_8_slpe_scores vs old day_8_train_slpe_scores Top100=39%；old day_8_slpe_scores vs current recompute Top100=38%；old day_8_train_slpe_scores vs current recompute Top100=97%；current real_slpe链路与 current train-style链路 Top100=100%（分数一致）。
2026-04-06: 定位“SLPE跑不出来”高概率触发点：scripts/README.md 仍含旧的无seed模型路径示例（conformer-5days / conformer-7days），而当前落盘目录为 conformer-{N}days-seed{seed}。已修正文档关键命令与路径说明为带 seed0 的默认示例。
2026-04-09: 已清理代码中的 L2SP/梯度裁剪相关链路：main_pipeline 与 launch_abcd_5seeds 移除 l2sp CLI/传参，src/model_a/finetune 移除 L2SP 正则逻辑与相关日志，相关调度脚本去除 grad_clip/l2sp 参数与文案；scripts 下代码/文档仅 outputs/results.md 仍保留历史实验描述。
2026-04-09: 验证 run_finetune_from_config.py 与 run_modelb_lr_sched_clip_sweep.py 在当前仓库可执行（语法/CLI/调度烟测通过）；run_finetune_from_config 需传微调配置 config.pkl，不能直接用 outputs/model_train 下训练配置（缺少 finetuneDataPath 与 pretrainedModelOutputPath）。
2026-04-09: 确认 launch_abcd_5seeds.py 最终会调用 src.model_a.finetune.finetune_model（链路：launch_abcd_5seeds -> main_pipeline finetune_only -> run_finetune_pipeline -> finetune_module.finetune_model_a -> finetune_model）。
2026-04-09: 检查 launch_abcd_5seeds 链路（launch_abcd_5seeds -> main_pipeline -> finetune_module -> src/model_a/finetune）：无 L2SP 代码与参数；梯度裁剪仅剩“grad_clip=off”日志和注释，未执行任何 clip_grad_* 操作。
2026-04-09: 检查 SLPE 实现链路：main_pipeline/launch_abcd_5seeds 在 finetune_method=real_slpe 时会进入 finetune_module.select_samples_for_finetune -> src/utils/sample_selection.real_slpe_sample_selection -> compute_real_slpe_scores -> src/utils/slpe.compute_slpe_batch；random/full_data 不走该链路。
2026-04-09: Model B 支持“加载已训练模型用于预测/选样”（通过 model_b_path），但当前无继续训练的 resume 参数；对路径 scripts/outputs/model_b/slpe-5days-seed0 实盘检查仅有 adapter_config.json+README.md，缺 head.pt/权重文件，按现有加载器无法成功加载。
2026-04-09: 用户需复现“model_b选100句，day8-12微调，关闭额外模块，10 seeds”；现有脚本无完全一键仅该子集，建议直接用 main_pipeline 循环命令（seed×day）执行，显式传 --finetune_method model_b --num_samples 100 --pretrained_ndays_eval 7 --no-use_adapter --no-use_ca_block 与每seed对应 model_a/model_b 路径。
2026-04-09: 核实当前 finetune 链路中 output_tag 不会改写结果目录（get_finetune_config 仍写 base_dir/outputs/model_test/...）；如需新目录，需改 base_dir 结构或对 outputs/model_test 做软链接/目录映射，日志可用 shell 重定向到新目录。
2026-04-09: 提供“model_b 100句 day8-12 seed0-9”并行 bash 脚本方案：支持自定义 GPU 列表与每卡并行度，通过新 run_base_dir（含 data 软链接）实现结果落到新目录，并将每任务日志写入 run_base_dir/logs。
2026-04-09: 新增 scripts/run_modelb100_parallel.py：并行调度 model_b 100句(day8-12, seed0-9)，支持 --gpus/--per_gpu，结果写入新 run 目录（base_dir=run_base）并自动链接 data，日志写 run_base/logs，输出 tasks.csv 与 summary.json。
2026-04-09: 定位并修复 run_modelb100_parallel 全失败根因：main_pipeline 导入链中 model_b_data_module 依赖 src.model_a.config.SESSION_NAMES_CHRONOLOGICAL，但该常量缺失导致 ImportError。已在 src/model_a/config.py 恢复常量并让 get_base_config 复用；现 main_pipeline import 正常。
2026-04-10: my_repro_v2（model_b 100×day8-12×seed0-9）统计时 finetuned_cer.txt 47/50；缺 day11-seed8、day12-seed8、day12-seed9（日志末尾仍在 batch 训练，未完成落盘）；已产出 47 条：全局 mean CER≈0.397，pstdev≈0.043；按天 mean：day8≈0.353、day9≈0.347、day10≈0.424、day11≈0.422(n=9)、day12≈0.455(n=8)。日志含 HF tokenizer 走代理失败重试（127.0.0.1:7890），但最终多数任务仍完成。
2026-04-10: 新增 scripts/run_realslpe100_parallel.py：并行调度 real_slpe + num_samples=100、day8-12、默认 10 seeds；可 --gpus/--per_gpu、独立 run 目录、可选 --use_slpe_cache。
2026-04-10: real_slpe 后台跑法：mkdir run 目录后 nohup python run_realslpe100_parallel.py ... >> outputs/repro_runs/<tag>/launcher_nohup.log 2>&1 &，PID 可写入 launcher.pid；单任务日志仍在 run_tag/logs/。
2026-04-10: 已杀停 run_model_a_train_parallel 及全部 project3 scripts/main_pipeline.py 子进程（多轮 kill -KILL直至 ps 无残留）。
2026-04-10: 新增 scripts/run_model_a_train_parallel.py：并行 train_model_a，落盘 outputs/model_train；--gpus/--per_gpu、默认 num_seeds=40，可 --discover_ndays 从已有目录解析 n_days；有 modelWeights.pth 默认跳过（--overwrite 重训）。日志 scripts/outputs/model_a_parallel_logs/<tag>/。
2026-04-10: my_repro_v3 outputs/model_test/*/real_slpe（50/50）：CER 全局 mean≈0.3976、std≈0.0407；按目录 7-8≈0.355、7-9≈0.349、7-10≈0.421、7-11≈0.412、7-12≈0.450（各 10 seed）；Loss 全局 mean≈2.97。
2026-04-11: 指标修正——对每个训练天数 N 的 Model A（40 seed），在排序后 sessionNames 的第 N+1、N+2 天 test 上算 CER，取 cer_{N+1}+cer_{N+2}（越小越好）。脚本 scripts/batch_evaluate_model_train_next2_transfer.py，结果 CSV outputs/model_train_next2_transfer_cer.csv。各 N 最优/次优 seed：N=1→35/37；N=2→37/15；N=3→35/24；N=5→2/34；N=7→19/2；N=9→1/16；N=10→35/31；N=12→15/38。
2026-04-11: 上述 16 个 checkpoint 已 cp -a 到 outputs/model_train_selected/；目录已重命名为 conformer-{N}days-best / second，对应关系见同目录 seed_mapping.txt。
2026-04-11: 按用户要求修改 scripts/launch_abcd_5seeds.py 为“selected best 模型版”：不再循环 model_a seeds，统一读取 outputs/model_train_selected/conformer-{N}days-best；ABCD 四组默认跑 random100/full_data/real_slpe100，其中 random100 跑 10 个 selection_seed（0-9），full/slpe 各跑 1 次；新增 --run_seed，日志根目录改为 scripts/outputs/automated_experiments/logs_abcd_selected_best。
2026-04-11: 按用户要求扩展 launch_abcd_5seeds 支持 model_b100（每个 day 1 次）且无需手工传 model_b_path：脚本按组自动推断 scripts/outputs/model_b/{slpe-groupA-1days|slpe-groupB-3days|slpe-5days|slpe-groupD-9days}-seed{model_b_seed}，新增 --model_b_seed（默认0）与 --model_b_root 参数；组合任务 random×10 + full/slpe/model_b×1 已 dry_run 验证（ABCD 总任务 221）。
2026-04-11: launch_abcd_5seeds 新增 --run_base_dir：支持将日志与结果统一写入指定目录（示例 outputs/repro_runs/my_repro_v4）。实现为日志写 <run_base_dir>/logs，main_pipeline 的 --base_dir 指向 run_base_dir（结果写 <run_base_dir>/outputs），并自动创建 <run_base_dir>/data -> /root/25S151115/project3/data 软链接；dry_run 验证通过。
2026-04-12: 核对 my_repro_v4 日志（run_20260411_085318）：`最终微调CER` 来自 evaluate_cer(eval_split="test")；`训练最佳CER` 来自训练中每100 batch在 test_loader 的最优值。出现 full_data_D_day21 训练最佳 0.2985 但最终 0.3729，根因是输出目录冲突：full_data(method=random,num_samples=-1) 与 random100(method=random,num_samples=100, selection_seed=0..9) 同写 `outputs/model_test/12-21/random/seed0`，并行任务互相覆盖 checkpoint/结果；day14 接近一致仅属未被明显覆盖或覆盖较小。
2026-04-12: 按用户要求将 full_data 相关 random 词条改为 full_data：config.get_finetune_config 中 method=random 且 num_samples<=0 时目录标签改为 full_data（`.../model_test/{N}-{day}/full_data/seed{seed}`），并将 finetune_module 的方法打印显示为 full_data；训练逻辑不变。
2026-04-12: 按用户要求更新 random 日志/目录记录方式：将 selection_seed 透传到 get_finetune_config，并在 random 且 num_samples>0 时将输出目录改为 `.../{method_tag}/seed{seed}_sel{selection_seed}`；避免 random100 不同 selection_seed 任务共享同一路径覆盖结果。full_data 仍为 `.../full_data/seed{seed}`。
2026-04-12: 汇总 logs/run_20260412_055234：共 187 条日志（仅 full_data 17 + random100 170），无 real_slpe100/model_b100；全部含「最终微调CER」；训练最佳与最终 CER 无 >0.02 偏差。D 组 full_data 明显优于 random100 均值（例 day21 full 0.2985 vs random 均值 0.3497）；日志已显示方法 full_data 与独立输出目录。
2026-04-12: 按 target day 汇总该 run 的 CER：day 3,6,7,8–12,13–21 共 17 天；每天 1 条 full_data + 10 条 random100（ssel0–9）；表中给出 full_data 与 random100 均值/极差（见对话）。
2026-04-12: random100 按 selection_seed 分段：ssel0–4 与 ssel5–9 各 5 条/天，在 logs/run_20260412_055234 上算每日均值（见对话表）。
2026-04-12: launch_abcd_5seeds.py 新增 --model_train_selected_root（默认 outputs/model_train_selected），用于指定 conformer-{N}days-best 所在目录；与 --run_base_dir 独立。用户示例：run_base_dir=repro_runs/my_repro_v5，model_train_selected_root=outputs/model_train_selected_2。
2026-04-12: 读取 my_repro_v5 当前结果（logs/run_20260412_132921）：已解析 221 日志，其中 6 条无“最终微调CER”（均为 model_b100：C day8-12 与 D day13）；其余已汇总成按组/天的 random100均值、real_slpe100、model_b100、full_data 表。总体相对 random100 的平均改变量：real_slpe100 -0.0032，model_b100 -0.0065（仅11天），full_data -0.0470。
2026-04-12: 按用户需求扩展 scripts/launch_abcd_5seeds.py，新增并列指标变体 real_cer100（method=real_cer, num_samples=100），与现有 real_slpe100 可并列通过 --variants 选择；脚本语法检查已通过。
2026-04-12: 修复 real_cer100 运行报错 TypeError(unexpected keyword 'cache_dir')：在 scripts/finetune_module.py 的 real_cer 包装调用中，将传给 src.utils.sample_selection.real_cer_sample_selection 的参数名由 cache_dir 改为 save_dir；语法与 lint 均通过。
2026-04-12: 读取 outputs/model_train/conformer-1days-seed0/results/trainingStats.pkl：dict 含 testLoss/testCER 各长度51（float64 数组）、bestEpoch=36、bestCER≈0.4978；testCER 最小值在索引35，与 bestCER 一致（bestEpoch 似为 1-based 或 epoch 编号）。
2026-04-12: 按各 N 天 40 seed 的 trainingStats.pkl「bestCER」最小选模，已 cp -a 到 outputs/model_train_selected_2（conformer-{N}days-best/second），对应关系见同目录 seed_mapping.txt（与 model_train_selected 的 next2 转移指标不同）。
2026-04-13: 按 outputs/model_train_next2_transfer_cer.csv 以“每个 n_days 下 40 seeds 的后两天 test CER 之和（cer_{N+1}+cer_{N+2}）最大”为准筛选最差迁移 seed：N=1→19，N=2→4，N=3→29，N=5→29，N=7→39，N=9→21，N=10→37，N=12→6。
2026-04-13: 已将最差迁移 seed 对应模型复制到 outputs/model_train_selected_3，并重命名为 conformer-{N}days-best（N=1/2/3/5/7/9/10/12）；新增 outputs/model_train_selected_3/seed_mapping.txt 记录目录映射（worst）。
2026-04-13: 已创建软链接 outputs/repro_runs/my_repro_v6/data -> outputs/repro_runs/my_repro_v5/data（非物理复制），并验证链接存在。
2026-04-13: 汇总 my_repro_v6 当前结果（run_20260413_104400）：日志39条，已有最终CER 32条、缺失7条（C_day9相关为主：full_data_C_day9、real_slpe100_C_day9、random100_C_day9_ssel1-4，另 model_b100_C_day8）。已产出按方法/按day统计：full_data 均值优于 random（day3/6/7/8均更低）。
2026-04-14: 再汇总 my_repro_v6（run_20260413_104400）：日志64条，含最终CER 59条、缺5条（均为 model_b100_C_day8–12）。finetuned_cer 共59条；按方法均值 full_data≈0.353、real_slpe≈0.392、model_b≈0.383（仅A/B/D组3条）、random≈0.408；按天 random 均值 day3–12 均有5条，full_data/slpe 各天均有，C 组 model_b 未完成。
2026-04-14: 按用户需求实现“model_b100 直接重训不加载”：main_pipeline.py 新增 --auto_train_model_b/--force_retrain_model_b/--model_b_output_dir；finetune_only+model_b 可先自动训练再微调。launch_abcd_5seeds.py 新增 --retrain_model_b，开启后 model_b100 不再传 --model_b_path，而是每任务传自动重训参数（含组别对应 train_days/val_days 与 model_a_path_train_b）。dry-run 与命令构造验证通过。
2026-04-14: 按用户需求扩展 random 子集规格：launch_abcd_5seeds.py 新增 variants=random150/random200/random300/random400（行为与 random100 一致：按 selection_seeds 分任务）。并在 src/model_a/finetune.py 增加样本不足提示：当请求数>=可用总数时打印“using 实际总数”；当预选索引有效数与请求数不一致时打印调整后的实际样本数。已 py_compile + dry-run 验证通过。
2026-04-14: 新增 scripts/export_abcd_slpe_table.py：按 ABCD 组与目标天导出 SLPE 明细表（字段含 group/day/session/model_a_eval_ndays/sample_index/slpe_score/slpe_rank_desc/transcription/phoneme_ids/phoneme_text），支持自定义 model_train_selected_root/base_dir/output_dir。已烟测 A 组 day3 成功，输出到 outputs/analysis/slpe_tables_smoke/。
2026-04-14: 修复 random 变体结果目录冲突：src/model_a/config.py 的 get_finetune_config 现在优先用 output_tag 作为 method_tag，因此 random100/random150/random200/random300/random400 会分别写入独立目录（如 .../random150/seedX_selY），不再与 random100 共用 random 目录。
2026-04-14: 按用户要求调整分组：launch_abcd_5seeds.py 新增 B1 组（day8），配置为 eval_ndays=5，model_b 使用 B 组路径模板 slpe-groupB-3days-seed{seed}；并将 C 组天数改为 day9-12。dry-run 与 build_tasks 命令验证通过：B1 day8 的 real_slpe/model_b 均使用 conformer-5days-best，model_b100 使用 B 组 model_b 路径。
2026-04-14: 按用户校正修改 C/C1 的 Model B 重训天数：当 model_b_pretrained_ndays=5、eval_ndays=7 时，launch_abcd_5seeds.py 现使用 model_b_train_days=[6,7]（原为 [3,4,5,6,7]）。已通过 build_tasks 验证，重训命令包含 --pretrained_ndays_train_b 5 --train_days 6 7 --val_days 8 9 10 11 12。
2026-04-14: 按用户要求将“同一逻辑”应用到其他组：统一为 model_b_train_days=[model_b_pretrained_ndays+1 .. eval_ndays]。当前配置验证为 A=2；B/B1=4,5；C/C1=6,7；D=10,11,12。已通过 build_tasks 命令检查各组生成参数。
2026-04-14: 汇总 my_repro_v6 的 run_20260414_051100（B1/day8）8条日志：full_data CER=0.361854（最佳）；model_b100 CER=0.397504，且与真实SLPE Top100 重合87%，与真实CER Top100 重合25%；real_slpe100 CER=0.407308；random100 五次 CER=[0.411765,0.406417,0.400178,0.410873,0.414439]，均值0.408734，std≈0.004998（best=0.400178, worst=0.414439）。
2026-04-14: 回答“model_b100 / random100 是否用了额外模块”：launch_abcd_5seeds 仅调 main_pipeline；在 finetune_only 中 random100 走 finetune_module 的 random_sample_selection（不需要 model_b_path、不触发 auto_train_model_b）；model_b100 走 model_b_sample_selection，且需 model_b_path 或启用 auto_train_model_b（会调用 model_b_data_module/model_b_train_module 先训 Model B）。
2026-04-14: 用户 уточ问“ca block 和 adaptffn”：launch_abcd_5seeds 对 random100/model_b100 均固定追加 --no-use_adapter 与 --no-use_ca_block，默认都关闭这两个模块；仅当通过 extra_args 末尾显式传相反开关时才可能覆盖。
2026-04-14: 误删 scripts/launch_abcd_5seeds.py 后已完整恢复文件并通过 `python -m py_compile` 语法检查；ReadLints 对该文件无报错。
2026-04-14: 用户询问“跑 C1/C2 且开启 adapter+ca_block 时默认超参”：当前链路默认来自 get_finetune_config（lrStart=5e-5, lrEnd=1e-6, scheduler_type=linear），PEFT sigma 由 get_model 兜底 adapter_init_scale=1e-2、ca_init_scale=1e-2；launch 脚本默认传 --no-use_adapter/--no-use_ca_block，需用 extra_args 追加 --use_adapter --use_ca_block 覆盖。
2026-04-14: 用户要“类似 terminal5 的可运行命令”，并额外指定 adapter+ca_block 与 lr 3e-4→1e-5；已给出 launch_abcd_5seeds 命令模板，使用 --extra_args -- --use_adapter --use_ca_block --lrStart 3e-4 --lrEnd 1e-5（当前 CLI 无 --lr_scheduler，默认 linear）。
2026-04-14: 运行报错 `unrecognized arguments: -- --use_adapter ...`；修正为 `--extra_args --use_adapter --use_ca_block --lrStart 3e-4 --lrEnd 1e-5`（去掉多余的独立 `--`）。
 2026-04-14: model_b100_C1/C2 失败根因确认：并非路径拼错，而是加载 `scripts/outputs/model_b/slpe-5days-seed0` 的 LoRA 配置与本机 `peft==0.6.2` 不兼容（adapter_config.json 含 `corda_config` 字段）。同时 launcher_20260414_160352 显示 `Retrain Model B for model_b100: False`，未启用 `--retrain_model_b`，因此没有走新产出的兼容 adapter_config。
 2026-04-15: zsh `parse error near '&'`：多因行尾反斜杠后带空格导致续行失效，下一行以 `>` 开头被误解析；或 `mkdir` 与 `nohup` 粘成一行（无换行）。修复：每行 `\` 后勿留空格；或把重定向与 `&` 与最后一参同写一行；或分两行先 mkdir 再 nohup。
2026-04-14: 汇总 my_repro_v6 的 model_test/7-13 与 7-14 两天 CER：7-13 full_data=0.371747、real_slpe100=0.448885、random100(5次均值)=0.443309；7-14 full_data=0.354473、model_b100=0.399773、real_slpe100=0.399773、random100(5次均值)=0.420838。两天平均（可比方法）full_data=0.363110、real_slpe100=0.424329、random100=0.432073。
2026-04-15: 新增 scripts/summarize_repro_train_best_cer.py：从 repro 日志解析「训练最佳CER」（不用测试集 Evaluation 行），按 day×样本量(150/200/300/400)×模型(A/B/C) 对 ssel0–4 聚合 mean/std/min/max；默认日志根为 logs/run_20260414_025738；`--brief` 仅输出聚合行、不写任何结果文件。
2026-04-15: summarize_repro_train_best_cer.py 文件名正则扩展为 `random{n}_{TAG}_day{d}_ssel{s}.log`（TAG 可为 A/B/C 或 B1/C2 等），以支持 logs/run_20260415_063443。

2026-04-15: 汇总 run_20260415_045059 的 D 组训练最佳CER（从日志“Best CER/训练最佳CER”提取）：按 day15-21 给出 full_data/model_b100/real_slpe100 与 random100(ssel0-4)日均。random100 日均分别为 day15=0.334340、day16=0.361900、day17=0.377780、day18=0.374340、day19=0.371140、day20=0.369200、day21=0.345920；各方法最优日（按日均最小）分别为 full_data=day21(0.281400)、model_b100=day15(0.321100)、real_slpe100=day15(0.320200)、random100=day15(0.334340)。

2026-04-15: 汇总 logs/新D组加模块modelb（model_b100, D day15-21）: 最终CER依次为 day15=0.320200, day16=0.306900, day17=0.375661, day18=0.351431, day19=0.354767, day20=0.489635, day21=0.312563；均值0.358737（std≈0.0583），最佳day16、最差day20。与 run_20260415_045059 的 D组model_b100对比，逐天delta为[-0.000900,-0.037900,+0.007407,-0.015795,+0.000000,+0.097730,-0.039196]，总体均值+0.001621（略差，主要受day20异常拉高）。

2026-04-15: 汇总 my_repro_v6/logs 下 tiaocan3e-4--1e-5 与 tiaocan5e-4--5e-5（仅训练最佳CER）：各20条日志中 model_b100 与 real_slpe100 各10条。按“每方法最优值”比较：3e-4→1e-5 时 model_b100=0.2919、real_slpe100=0.2976；5e-4→5e-5 时 model_b100=0.2976、real_slpe100=0.3004。整体上 3e-4→1e-5 略优于 5e-4→5e-5（两方法各优约0.0028），且在两组学习率下 model_b100 的最优值都略低于 real_slpe100。
2026-04-15: 按用户要求给出 tiaocan3e-4--1e-5 与 tiaocan5e-4--5e-5 全部日志（每一天）训练最佳CER清单，仅罗列不比较；提取规则为优先匹配“Best CER”，缺失时回退“训练最佳CER”（如 real_slpe100_C1_day12=0.4145）。

2026-04-16: my_repro_v6/logs/C1C2_length100/length100 共6条日志训练最佳CER：C1 day9=0.3610、day10=0.4225、day11=0.4226、day12=0.4550；C2 day13=0.4219、day14=0.4032。
2026-04-16: 按用户要求提取 my_repro_v6/logs/length3e-4--1e-5/length100 与 length5e-4--5e-5/length100 全部训练最佳CER（各5条，C1 day10-14）：3e-4--1e-5 为[0.4175,0.4103,0.4311,0.4117,0.3964]；5e-4--5e-5 为[0.4183,0.4103,0.4301,0.4126,0.3930]（按 day10→day14）。

2026-04-16: 用户要求不看day9；基于 results.md(145-160,188-195) 统计 day3/6/7/8/10/11/12/13/14，按每天取 model_b/slpe 最优（原始+两组搜参）相对 random 的非分组平均优化：model_b=-0.0164，slpe=-0.0134。
2026-04-16: 已为 model_b/real_slpe 增加 strategy 选样：默认 hard_top100；新增 ran_x_y_z（按难/中/易三段随机，x+y+z=100，且仅支持 num_samples=100）。main_pipeline 新增 --selection_strategy 并做 ran_x_y_z 校验；launch_abcd_5seeds 新增 --strategy 支持多值（如 ran_70_30_0 ran_60_20_20），对 ran 策略按 --selection_seeds 展开多任务。finetune/sample_selection 已打通 ran 策略与 selection_seed。README/MODULE_FRAMEWORK 已补充示例。
2026-04-17: 按用户需求扩展 launch_abcd_5seeds 的 model_b 变体：新增 model_b50/model_b150/model_b200（保留 model_b100），均映射 method=model_b 并通过 num_samples 控制 top 句数；同步更新脚本文档注释、--retrain_model_b 帮助文案与启动打印为 model_b*；py_compile 校验通过。
2026-04-17: 修复 model_b 选样调用报错 `TypeError: model_b_sample_selection() got an unexpected keyword argument 'seed'`：在 scripts/finetune_module.py 的本地包装函数 model_b_sample_selection 增加 seed 参数并透传到底层 src.utils.sample_selection.model_b_sample_selection；py_compile 校验通过。
2026-04-17: 按用户要求完善 seed 语义（与 ran_x_y_z 对齐）：scripts/finetune_module.py 中 model_b 分支改为仅当 selection_strategy 以 ran_ 开头时使用 seed，hard/easy/diverse 强制使用 strategy_seed=0，避免非随机策略受 seed 影响；并更新相关参数注释。
2026-04-17: 按用户要求同步更新 launch_abcd_5seeds 日志文案：启动打印由 `Selection seeds (random)` 改为 `Selection seeds (random/ran_x_y_z)`，与当前 seed 适用范围一致；py_compile 校验通过。
2026-04-21: 回答“不同 run_seed 是否输出到不同目录”：会。launch_abcd_5seeds 将 --run_seed 透传为 main_pipeline 的 --seed；get_finetune_config 目录包含 seed_tag（.../outputs/model_test/{N}-{day}/{method_tag}/seed{seed}，random 子集为 seed{seed}_sel{selection_seed}），因此不同 run_seed 会落到不同结果目录（日志目录仍由 run_base_dir/log_tag 决定，不自动按 run_seed 分目录）。
2026-04-21: 回答“run_seed 与 selection_seed 是否冲突（用户给的 model_b100 命令）”：不冲突但语义不同。run_seed 控制训练随机种子并决定结果目录中的 seed{run_seed}；selection_seed 仅在 random 或 ran_x_y_z 采样时生效。用户命令里 `--run_seed 1 2 3 4` 与脚本参数类型不匹配（当前仅接受单个 int），会报参数错误；且 `model_b100 + 默认 hard_top100` 下 selection_seed 实际不参与选样。
2026-04-21: 已实现“多 run_seed 自动展开 + seed 分类分目录保存”：launch_abcd_5seeds 的 `--run_seed` 改为可多值（nargs+），build_tasks 按 run_seed 外层展开并在任务名/日志路径中写入 rseed；日志路径改为 `<logs>/run_seed{n}/{variant}/...`。同时 get_finetune_config 输出目录改为 `<.../{method_tag}/run_seed{n}/[selection_seed{m}]>`，selection_seed 不再与 run_seed 混在同一级命名，model_b/real_slpe 的 ran 策略也能按 selection_seed 分目录避免覆盖。
2026-04-21: 用户要求把现有 v9 启动命令改为“开启 adapter+ca_block + 特殊学习率”并提供对应 real_slpe 版本；建议通过 `--extra_args --use_adapter --use_ca_block --lrStart 3e-4 --lrEnd 1e-5` 覆盖 launcher 默认 `--no-use_*`，并分不同 run_base_dir/log_tag 启动 model_b100 与 real_slpe100 两条 nohup 命令。
2026-04-22: 汇总 my_repro_v6 指定三处 random100 日志（原始ABC组实验 + B组第8天实验 + C组13/14天实验），只取“训练最佳CER”；按组内每天对 5 个 ssel(0-4) 计算 mean±std：A day3=0.4949±0.0034，B day6=0.3027±0.0043，B day7=0.3956±0.0080，B day8=0.4087±0.0050，C day8=0.3515±0.0031，C day9=0.3696±0.0050，C day10=0.4485±0.0034，C day11=0.4332±0.0092，C day12=0.4673±0.0055，C day13=0.4433±0.0023，C day14=0.4208±0.0079。
2026-04-22: 补充汇总 my_repro_v6/logs/新D组实验/random100（35条，day15-21，每天ssel0-4）训练最佳CER的 day-level mean±std：day15=0.3343±0.0073，day16=0.3619±0.0083，day17=0.3778±0.0072，day18=0.3743±0.0049，day19=0.3711±0.0057，day20=0.3692±0.0085，day21=0.3459±0.0084。
2026-04-22: 按用户要求将 v9_realslpe 启动片段改为 full_data 版本：`--variants` 从 `real_slpe100` 改为 `full_data`，并建议同步调整输出目录与 log_tag（`my_repro_v9_fulldata` / `ABCD-fulldata`）以避免与旧 real_slpe 结果混淆。
2026-04-22: 核查论文“Implementation Details modelb”与代码实现一致性：当前仓库主训练为 RoBERTa/LoRA 的 Model B（rank 归一化 + MarginRankingLoss，默认 batch_size=16, lr=2e-5, head_lr=1e-3, num_epochs=10, AdamW weight_decay=0.01, linear warmup+decay），并无“historical decoder/余弦调度/100 epochs/BERT-base+phoneme-length MLP/alpha-beta分层采样/低秩语义适配器冻结主干”等实现；另 Model A 输出为 41 类（40 音素+CTC blank）。
2026-04-22: 核查 outputs/model_train_selected_3/conformer-1days-best/results/trainingStats.pkl：仅含 testLoss/testCER/bestEpoch/bestCER，不包含学习率字段；learning rate 只能从训练配置（config.pkl）或训练日志确认。
2026-04-23: 从 repro 日志仅解析「训练最佳CER」，对 run_seed1–4 聚合 mean±std（样本标准差）；my_repro_v9 ABCD-modelb-mutiseeds 的 C 组(day10–14) 日志在加载 slpe-5days-seed0 LoRA 时因 peft 与 corda_config 不兼容崩溃，无训练最佳行；其余目录统计完整。
2026-04-23: 按用户要求将 results.md 第189-194行（C组不同句子对比）从“seed0+seed1-4合并均值±方差”恢复为原始单值版本：day10-14 的 model_b50/100/150/200 分别恢复到 0.4400/0.3958/0.3833/0.3575、0.4444/0.4033/0.3815/0.3727、0.4559/0.4265/0.4081/0.3906、0.4452/0.4136/0.3736/0.3680、0.4236/0.3556/0.3635/0.3352。
2026-04-23: 按用户要求补齐 results.md 主实验结果(A/B/C/D)中的 direct transfer（不微调直接迁移）字段：从同文件“不微调，性能随天数下降（10 seeds）”表按组映射读取（A用2days→day3，B用5days→day6-8，C用7days→day10-14，D用12days→day15-21），并在每个 day 行中新增 `direct transfer mean±std` 及相对 random100 的差值（+Δ）。
2026-04-24: 按用户要求仅基于 outputs/model_train_selected_3（指定单模型）重新计算 A/B/C/D 组 direct transfer（目标天 test CER），并更新 scripts/outputs/results.md 第143-171行对应 direct transfer 数值与相对 random100 的差值；口径由“10 seeds 均值±方差”改为“单模型单值”。
2026-04-24: 按用户要求汇总多目录日志到 scripts/outputs/result.txt，用于后续按 seed 计算 std；新格式为 CSV 明细（group/day/method/run_seed/selection_seed/strategy/best_cer/source/status/log_path）+ 缺失 best CER 列表。扫描目录：my_repro_v6/v7/v8/v9 及 my_repro_v9_fulldata、my_repro_v9_modelb_ca_adapter_lr_special、my_repro_v9_realslpe；匹配 1029 条日志，成功提取 984 条，缺失 45 条。
2026-04-24: 按用户要求基于 scripts/outputs/result.txt 生成“results.md(142-206) 对应种子展开明细”，并写入 result.txt 顶部：包含主实验、随机样本量对比、C组 model_b50/100/150/200、C组模块消融；明细按 ssel/run_seed 展开，direct transfer 保留指定单模型值并标注 model_seed。
2026-04-24: 按用户要求为 my_repro_v7/logs/C1_slpe_modelb_ran_mix_v1 下 real_slpe100 与 model_b100 两目录做单独总结，并追加到 scripts/outputs/result.txt 底部：按 day×strategy×ssel 列出 Best CER，统计 total/ok/missing，并列出缺失 best CER 的日志文件（两目录均 total=105, ok=96, missing=9）。
2026-04-24: 按用户要求修正 result.txt 第56-61行“模块消融实验：C组（种子展开）”：将“model b + ca block + adapter”从旧来源替换为 my_repro_v9/logs/C1-model-b-advanced-50-150-200 的加模块结果，并按 day10-14 展开 model_b50/model_b150/model_b200 的 seed0-4（seed0来自 my_repro_v8 对应实验，seed1-4来自 my_repro_v9）。
2026-04-24: 按用户要求将 scripts/outputs/result.txt 第50-54行里的 model_b100 改为引用同文件第17-22行 model_b_advanced 的种子展开数值（day10-14）。
2026-04-24: 按用户要求修正 scripts/outputs/result.txt 第50-54行“对比试验：C组 model b + ca block + adapter 不同句子（种子展开）”：将 model_b50/model_b150/model_b200 的 seed1-4 更新为 my_repro_v9/logs/C1-model-b-advanced-50-150-200 日志中的“训练最佳CER”；model_b100 seed1-4 与 C1-model-b-only-100-rerun 日志一致，保持不变。
2026-04-24: 按用户要求重算 scripts/outputs/results.md 主实验区段（A/B/C/D，day3/6-8/10-14/15-21）中“相对 random100 的括号 delta”为 seed 级差值统计：对有多 seed 的方法按同索引计算 (method_seed_i - random100_ssel_i) 后汇总 mean±std；单 seed 方法（如部分 slpe）保留单值 delta。已更新 results.md 第145-170行对应括号内容。
2026-04-24: 按用户要求读取 my_repro_v10/logs/C1-real-cer100 的 25 条日志，提取每个 day(10-14) × run_seed(0-4) 的“训练最佳CER”（取日志内 batch 行 cer 最小值），并追加写入 scripts/outputs/result.txt 新分节。
2026-04-24: 按用户要求在 scripts/launch_abcd_5seeds.py 新增变体 random50（method=random,num_samples=50）和 model_b300（method=model_b,num_samples=300），并同步更新脚本头部方法说明；`--variants` 现可直接选择这两项。
2026-04-24: 按用户要求提供启动命令模板：仅开启 ca_block（不启用 adapter）、使用 model_b100，并通过 extra_args 设置 lrStart=3e-4、lrEnd=1e-5。
2026-04-25: 按用户要求扫描 my_repro_v10 指定 7 个日志目录，提取每条日志的 day/run_seed/selection_seed 与训练最佳CER（优先匹配“Best CER”，回退“训练最佳CER”），汇总写入 scripts/outputs/result.txt（TSV）；共165条日志，15条缺失最佳CER已留空。
2026-04-25: 用户要求保持 result.txt 原文本结构；已在文件末尾按既有“day明细 + mean±std + missing列表”格式追加 my_repro_v10 指定7目录结果（未改动原有段落内容）。
2026-04-25: 按用户要求计算 results.md 中 slpe 相对 random100 的 seed级差值统计（先逐seed做 slpe-random100，再算 mean±std，口径为总体标准差）：A day3=-0.0211±0.0047；D day15-21 分别为 -0.0127±0.0107、-0.0137±0.0076、-0.0114±0.0073、-0.0030±0.0084、-0.0144±0.0072、-0.0124±0.0088、-0.0209±0.0088。
2026-04-25: 按用户授权将上述 slpe-random100 的 seed级差值统计直接回填 scripts/outputs/results.md：更新 A day3 与 D day15-21 的 slpe 括号项为 mean±std，其余字段保持不变。
2026-04-25: 按用户要求将 result.txt 中 C1-full-data-advanced-ca-adapter-lr3e4-1e5 的 day10-14 均值±方差整合到 results.md 第187-193行表格，右侧新增 full data 列，并补充 avg=0.3480±0.0193（按5天均值的总体std）。
2026-04-25: 按用户要求在 results.md 模块消融实验（C组 day10-14）中新增“model b + ca block”列（插入为倒数第二项，位于 model_b_advanced 前），数据来自 result.txt C1-model-b100-ca-lr3e4-1e5：day10-14 分别 0.3988±0.0077、0.3991±0.0036、0.4390±0.0095、0.4158±0.0030、0.3581±0.0045；括号为相对 random100 的差值（-0.0497/-0.0341/-0.0283/-0.0275/-0.0627）。
2026-04-25: 按用户要求将 result.txt 的 C1-badge100（day10-14）追加到 results.md 第223-231行区段下方，格式与 C1-real-cer100 一致，并补 all days mean±std=0.4434±0.0200。
2026-04-25: 按用户要求从 scripts/outputs/results.md 截取并新建汇总文件 scripts/outputs/results_extract_AD_slpe_badge_C_random50.md，包含 A/D 的 slpe、C1-badge100、C组 random50 以及 results.md(195-202) 的 C组对比表。
2026-04-25: 按用户追加要求更新摘录文件：新增 A/D 对应的 direct transfer 数值，并将该标签文案改为用户指定拼写“no fiuetune”。
2026-04-25: 按用户要求在摘录文件中新增 C组 no fiuetune（day10-14）条目，沿用“no fiuetune”拼写并保留括号中的相对 random100 差值。
2026-04-26: 按用户要求修改 result.txt 第233-258行对应的 C1-model-b-advanced-300-ca-adapter-lr3e4-1e5 段落：更正为 total=25, ok=25, missing=0，并补全 day10-14 的 seed0-4 训练最佳CER与 mean±std，删除错误的 missing 列表。
2026-04-26: 回答 run_seed 底层实现链路：launch_abcd_5seeds 外层展开 run_seed 生成任务并传给 main_pipeline 的 --seed；main_pipeline 在 finetune_only 中把 seed/selection_seed 传入 finetune_module；finetune_module 用 seed 控制训练与默认采样随机性，selection_seed 仅用于 random 与 ran_x_y_z，最终随机选样落到 src/utils/sample_selection.random.seed(seed) 与 ran 策略的 random.Random(seed)。
2026-04-26: 回答“微调是否只影响 DataLoader shuffle”：不是。seed 在 finetune 里还会影响 torch/numpy 随机流（含噪声增强、可能的dropout等）；若 selected_indices 已固定，则样本集合本身不再受 seed 影响，但 batch 顺序与训练随机过程仍受影响。
2026-04-26: 实现 down100 策略链路（launch_abcd_5seeds/main_pipeline/sample_selection）：支持 `--strategy down100` 表示“按预测分数最低100句”，并兼容 ran 写法 `ran33_34_33`（归一化为 ran_33_34_33）；dry_run 验证 C1+model_b100+run_seed0-4 正常展开。
2026-04-26: 修复 ran 任务启动即报错：`main_pipeline.py` 的 `normalize_selection_strategy` 注解由 `str | None` 改为 `Optional[str]`，兼容 Python<3.10，已通过 `python -m py_compile` 验证。
2026-04-26: 针对 `TypeError: unsupported operand type(s) for |: 'type' and 'NoneType'` 兼容性问题，已将 `scripts/launch_abcd_5seeds.py` 的解释器常量从 `python` 改为 `python3`，避免调度任务误用旧版本解释器；ReadLints 检查通过。2026-04-27: 按用户要求读取 my_repro_v10/logs/A-D-badge100、my_repro_ran50_30_20、my_repro_ran33_34_33、my_repro_down100 的日志，提取训练最佳CER（优先 Best CER，回退 Fine-tuned/训练最佳），并以 day×seed/ssel 明细+mean±std 形式追加到 scripts/outputs/result.txt 末尾。
2026-04-27: 按用户要求将 result.txt(257-313) 整理追加到 scripts/outputs/results.md 底部；新增 A-D-badge100 与 C1-ran50_30_20 / ran33_34_33 / down100 的 day级表格，并计算相对 random100 与 model b only 的差值。
2026-04-27: 按用户要求补齐 scripts/outputs/results.md 底部对比表（day10-14）中的 random100 与 model b only 的 std，数值对齐 C组主实验区段(161-166)。
2026-04-29: 定位 model_b 训练超参链路：optimizer 为 AdamW(weight_decay=0.01)；参数组 lr 分别为 bert/lora=2e-5 与 regressor head=1e-3（默认）；scheduler 为 linear warmup+decay，num_warmup_steps=500，total_steps=len(train_loader)*num_epochs。main_pipeline 的 train_only/full 当前未暴露学习率 CLI 参数，需在 run_model_b_training_pipeline 调用 train_model_b 时传 learning_rate/head_learning_rate 或扩展 argparse。
2026-04-29: 按用户要求修复 selection_seed 与 run_seed 对齐：launch_abcd_5seeds.py 新增按任务解析 seed 逻辑，random 与 ran_x_y_z 生效时令微调 seed=selection_seed（并同步到任务名/日志 run_seed 目录）；main_pipeline.py(finetune_only) 增加同口径兜底，手工调用时若 random/ran 且传 selection_seed 也会自动用该 seed 训练。
L141:2026-05-12: 初始化 GitHub 上传准备；新增 .gitignore，排除 data/logs/outputs 与 scripts/outputs 下指定大目录/文件，并补充忽略 __pycache__/*.pyc/*.log，避免无关大文件进入仓库。
